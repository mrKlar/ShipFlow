id: technical-framework-stack
title: Node REST service scaffold stays in place
severity: blocker
category: framework
runner:
  kind: custom
  framework: custom
app:
  kind: technical
  root: .
assert:
  - path_exists:
      path: package.json
  - path_exists:
      path: src/server.js
  - script_present:
      name: dev
  - script_present:
      name: start
