---
description: Legacy alias for the standard ShipFlow implementation loop
argument-hint: [optional focus area]
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, Agent
---

# ShipFlow — Legacy Alias

Use the same workflow as `/shipflow-implement`.
