id: technical-ci-stack
title: Example keeps Playwright and its local delivery contract
severity: blocker
category: ci
runner:
  kind: custom
  framework: custom
app:
  kind: technical
  root: .
assert:
  - path_exists: { path: package.json }
  - dependency_present: { name: "@playwright/test", section: devDependencies }
  - path_exists: { path: playwright.config.ts }
