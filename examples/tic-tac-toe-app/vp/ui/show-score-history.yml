id: show-score-history
title: Player sees persisted score history when the app opens
severity: blocker
state:
  kind: sqlite
  connection: ./test.db
  reset_sql: |-
    CREATE TABLE IF NOT EXISTS score_history (
      game_id INTEGER PRIMARY KEY,
      winner TEXT NOT NULL,
      moves_json TEXT NOT NULL,
      move_count INTEGER NOT NULL,
      finished_at TEXT NOT NULL
    );
    DELETE FROM score_history;
    INSERT INTO score_history (game_id, winner, moves_json, move_count, finished_at)
    VALUES (1, 'X', '[0,3,1,4,2]', 5, '2026-01-01T00:00:00.000Z');
app:
  kind: web
  base_url: http://localhost:3000
flow:
  - open: /
assert:
  - visible:
      testid: board-cell-0
  - count:
      testid: score-entry
      equals: 1
  - text_matches:
      testid: score-entry-0
      regex: X.*5
